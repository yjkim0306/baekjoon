// 와이버스 부릉부릉
#include <iostream>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int n, k;
    cin >> n >> k;
    for(int i = 0; i < n; i++)
    {
        int a, b;
        cin >> a >> b;
    }

    cout << "비와이\n";
    
    return 0;
}